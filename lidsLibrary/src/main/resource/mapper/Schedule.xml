<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lids.dao.ScheduleDao">

    <insert id="generateSchedule" statementType="CALLABLE">
        {call generate_schedule(
        #{0, mode=IN}
        )}
    </insert>

    <select id="getRoomSchedulers" resultType="java.util.Map">
        select space_id,date,begin_time,end_time,name
        from schedule inner join space on
        schedule.space_id = space.id
        where space.space_type_name = "标准研习间"
        AND DATE = #{0}
        order by date
    </select>

    <select id="getSeatSchedulers" resultType="java.util.Map">
        select space_id,date,begin_time,end_time,name
        from schedule inner join space on
        schedule.space_id = space.id
        where schedule.space_id = #{0}
        order by date
    </select>

    <select id="getAllSeatSchedulers" resultType="java.lang.Integer">
        SELECT space_id FROM
        schedule INNER JOIN space
        ON schedule.space_id = space.id
        WHERE space_type_name = "标准座位"
        AND date = #{0} AND begin_time &lt;= #{1}AND end_time &gt;= #{2}
    </select>

</mapper>